// 启用jetstream，空模块将启用并使用默认值 的jetstream
jetstream {
    // jetstream数据存放位置
    store_dir: "/opt/nats-workspace/nats-data/"
    // 1GB
    max_memory_store: 1073741824
    // 10GB
    max_file_store: 10737418240
}
